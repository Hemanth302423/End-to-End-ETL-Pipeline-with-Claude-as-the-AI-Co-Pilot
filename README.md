## Test of Claude generated pipeline ##

## WIP ##
Build a production-grade ETL pipeline that ingests raw public data, transforms it, and loads it into a local data warehouse — with Claude Desktop used visibly throughout (code generation, debugging, documentation).

Ingestion       : Python + requests / httpx
Transformation: : PySpark
Storage         : DuckDB

